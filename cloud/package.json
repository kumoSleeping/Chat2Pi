{
  "name": "chat2pi-cloud",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "wrangler deploy --dry-run --outdir ../.local/cloud-bundle && node test.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "miniflare": "^5.20260918.0-alpha",
    "typescript": "^5",
    "wrangler": "^4"
  }
}
